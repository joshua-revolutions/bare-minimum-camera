{
  "expo": {
    "name": "bare-minimum",
    "slug": "bare-minimum",
    "version": "1.0.0",
    "assetBundlePatterns": [
      "**/*"
    ],
    "plugins": [
      "react-native-vision-camera"
    ]
  }
}
